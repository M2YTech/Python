#now because we have f stings we do not use .format() anymore but some old codes still use 
# it so we should learn it in order to understand them
# .format() is a method of the str class that allows you to format strings by inserting values into placeholders
# it is used to create formatted strings by replacing placeholders with values  

a = "{} is a good {}".format("Muhammad", "Boy")
print(a)