"""
np.split() for splitting an array equally

np.hsplit() for horizontally splitting an array

np.vsplit() for vertically splitting an array
"""

import numpy as np

arr = np.array([10,20,30,40,50,60])

print(np.split(arr, 2))