#Boolean mastering
import numpy as np

arr = np.array([10,20,30,40,50,60])

print(arr[arr>25])

#used in machine learning for data filtering, it is 10 times faster then using loop for filtering data set 